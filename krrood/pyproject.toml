[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.pyrefly]
project-includes = ["**/*"]
project-excludes = ["**/*venv/**/*"]

[project]
name = "krrood"
dynamic = ["version"]
dependencies = [
    "rdflib",
    "jinja2",
    "inflect",
    "lemminflect",
    "owlrl",
    "typing-extensions>=4.12.2",
    "rustworkx>=0.14.0",
    "sqlalchemy",
    "black",
    "ruff",
    "sortedcontainers",
    "inflection",
    "anytree>=2.8.0",
    "ordered_set>=4",
    "pygraphviz>=1.7",
    "graphviz",
    "tabulate",
    "ipython",
    "requests",
    "colorama",
    "tqdm",
    "pygments",
    "pandas",
    "pyparsing>=3.2.3",
    "omegaconf",
    "pydot",
    "ucimlrepo>=0.0.7",
    "pdbpp",
    "pytest",
    "probabilistic_model",
    "random_events",
]
description = "A python package for Knowledge Representation and Reasoning systems based on Object Oriented Design."
readme = "README.md"
requires-python = ">=3.10"
license = "LGPL-3.0-only"
authors = [
    {name = "Abdelrhman Bassiouny", email = "abassiou@uni-bremen.de"},
    {name = "Tom Schierenbeck", email = "tom_sch@uni-bremen.de"}
]
keywords = ["Knowledge-representation", "reasoning", "ontology", "orm", "query-langauge", "database", "python"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0,<8.0.0",
    "mypy>=1.8",
    "matplotlib",
    "igraph",
    "pyyaml",
]
visualization = [
    "dash",
    "plotly",
    "flask",
]

[project.urls]
Homepage = "https://cram2.github.io/cognitive_robot_abstract_machine/krrood/intro.html"
"Source" = "https://github.com/cram2/cognitive_robot_abstract_machine"
"Bug Tracker" = "https://github.com/cram2/cognitive_robot_abstract_machine/issues"

[tool.setuptools]
package-dir = {"" = "src"}
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
include = ["krrood*"]
exclude = ["test*"]

[tool.setuptools.package-data]
krrood = [
  "ormatic/templates/*.j2",
  "ormatic/templates/*.jinja",
  "rustworkx_utils/visualization/templates/*.jinja",
  "code_generation/templates/*.jinja",
  "entity_query_language/testing/templates/*.jinja",
]

[tool.pytest.ini_options]
testpaths = ["test"]
pythonpath = ["src", "test"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
addopts = [
    "-v",
    "--strict-markers",
    "--tb=short",
]
markers = []

[tool.coverage.run]
source = ["src/krrood"]
omit = ["*/tests/*", "*/test_*.py"]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod",
]

[tool.setuptools.dynamic]
version = {attr = "krrood._version.__version__"}
